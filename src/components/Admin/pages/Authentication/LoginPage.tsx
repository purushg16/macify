import { Box } from "@chakra-ui/react";
import Title from "../../elements/Title";

const LoginPage = () => {
  return (
    <Box>
      <Title
        heading="Welcome Back, Captain"
        subtitle="Login using your credentials"
      />
    </Box>
  );
};

export default LoginPage;
